acl {
  # deny { user_agent.match(/Chrome/i) }

  # bots
  # deny { user_agent.match(/Applebot/i) }
  # deny { user_agent.match(/baiduspider/i) }
  # deny { user_agent.match(/Bingbot/i) }
  # deny { user_agent.match(/Googlebot/i) }
  # deny { user_agent.match(/ia_archiver/i) }
  # deny { user_agent.match(/msnbot/i) }
  # deny { user_agent.match(/Naverbot/i) }
  # deny { user_agent.match(/seznambot/i) }
  # deny { user_agent.match(/Slurp/i) }
  # deny { user_agent.match(/teoma/i) }
  # deny { user_agent.match(/Twitterbot/i) }
  # deny { user_agent.match(/Yandex/i) }
  # deny { user_agent.match(/Yeti/i) }
}
